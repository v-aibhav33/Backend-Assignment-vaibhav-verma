services:
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - data:/data
    environment:
      DATABASE_URL: sqlite:////data/app.db
      WEBHOOK_SECRET: ${WEBHOOK_SECRET}

volumes:
  data:
